{
  "blivedm_open_live": {
    "description": "B站开放平台接入（与Web接入二选一）",
    "type": "object",
    "items": {
      "enable": {
        "description": "是否启用",
        "type": "bool"
      },
      "access_key_id": {
        "description": "access_key_id",
        "type": "string"
      },
      "access_key_secret": {
        "description": "access_key_secret",
        "type": "string"
      },
      "app_id": {
        "description": "app_id",
        "type": "int"
      },
      "room_owner_auth_code": {
        "description": "room_owner_auth_code",
        "type": "string"
      }
    }
  },
  "blivedm_web": {
    "description": "B站Web接入（Y账号：直播间弹幕与视频评论回复都走此账号）",
    "type": "object",
    "items": {
      "enable": {
        "description": "是否启用",
        "type": "bool"
      },
      "room_id": {
        "description": "直播间ID",
        "type": "int",
        "hint": "直播间地址 live.bilibili.com/ 后面的数字，短号也可以。"
      },
      "cookie_SESSDATA": {
        "description": "Cookie: SESSDATA",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "浏览器登录bilibili.com后，F12 → 应用 → Cookie → www.bilibili.com 中复制（勿勾选『显示已解码的网址』）。发送弹幕/评论必须填写；泄露等于账号被盗。"
      },
      "cookie_buvid3": {
        "description": "Cookie: buvid3",
        "type": "string",
        "default": "",
        "hint": "设备标识，与SESSDATA同一位置复制；缺失时日志提示 Missing buvid。"
      },
      "cookie_bili_jct": {
        "description": "Cookie: bili_jct",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "发送弹幕/评论必须填写；与SESSDATA同一位置复制。"
      },
      "cookie_refresh_token": {
        "description": "Cookie: refresh_token（刷新口令）",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "开启Cookie自动刷新时必填：F12 → 控制台输入 copy(localStorage.ac_time_value) 回车获取；也可发送 /bililogin 扫码登录自动写入。"
      }
    }
  },
  "account_x": {
    "description": "B站X账号（只读轮询本账号收到的视频评论，转发给Y账号统一回复）",
    "type": "object",
    "items": {
      "enable": {
        "description": "是否启用X账号",
        "type": "bool",
        "default": false,
        "hint": "X账号只收不发，所有回复由Y账号发出，风控风险极低。"
      },
      "cookie_SESSDATA": {
        "description": "Cookie: SESSDATA",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "获取方法与Y账号相同；泄露等于账号被盗。"
      },
      "cookie_buvid3": {
        "description": "Cookie: buvid3",
        "type": "string",
        "default": "",
        "hint": "与SESSDATA同一位置复制。"
      },
      "cookie_bili_jct": {
        "description": "Cookie: bili_jct",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "X账号只读可不填；填写后可配合自动刷新维持登录态。"
      },
      "cookie_refresh_token": {
        "description": "Cookie: refresh_token（刷新口令）",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "获取方法同Y账号；不填则X账号Cookie过期后需手动更换。"
      }
    }
  },
  "comment_reply": {
    "description": "视频评论区自动回复（统一使用Y账号回复）",
    "type": "object",
    "items": {
      "enable": {
        "description": "是否启用视频评论区自动回复",
        "type": "bool",
        "default": false,
        "hint": "轮询Y账号（及启用的X账号）的『收到评论』，由LLM生成回复并用Y账号发布；不直播时也在运行。"
      },
      "video_context": {
        "description": "回复前识别所属视频内容",
        "type": "bool",
        "default": true,
        "hint": "根据视频标题/简介生成一句话概括并注入提示词，让AI了解讨论范围；按视频缓存，每个视频仅一次额外LLM调用。"
      },
      "poll_interval": {
        "description": "收到评论轮询间隔（秒）",
        "type": "int",
        "default": 180,
        "hint": "建议不低于120秒，过频请求可能被限制。"
      },
      "min_interval": {
        "description": "两条评论回复的最小发送间隔（秒）",
        "type": "float",
        "default": 45.0,
        "hint": "评论风控较严，勿调低；低等级账号建议再调大。"
      },
      "random_delay_max": {
        "description": "额外随机延迟上限（秒）",
        "type": "int",
        "default": 20,
        "hint": "每条回复发送前随机等待0~N秒，0为关闭。"
      },
      "max_replies_per_cycle": {
        "description": "每轮最多处理的新评论条数",
        "type": "int",
        "default": 3,
        "hint": "超出部分顺延到下一轮。"
      },
      "max_length": {
        "description": "单条评论回复最大长度（字）",
        "type": "int",
        "default": 120,
        "hint": "超出截断，建议保持100字左右。"
      },
      "max_reply_depth": {
        "description": "楼中楼对话最大回复深度（轮数）",
        "type": "int",
        "default": 3,
        "hint": "同一评论楼层下机器人最多回复的轮数，防用户无限追问套娃；0为不限制。"
      },
      "context_max_chars": {
        "description": "评论上下文单条最大长度（字）",
        "type": "int",
        "default": 120,
        "hint": "主评论/被回复评论注入提示词前的截断长度，防长评论撑爆上下文。"
      }
    }
  },
  "live_monitor": {
    "description": "开播监控（仅Web接入支持）",
    "type": "object",
    "items": {
      "enable": {
        "description": "开播才进房（轮询开播状态）",
        "type": "bool",
        "default": false,
        "hint": "开启后不常驻直播间：开播才连接弹幕，下播自动断开。"
      },
      "poll_interval": {
        "description": "开播检测间隔（秒）",
        "type": "int",
        "default": 30,
        "hint": "建议15~60秒。"
      },
      "notify_destinations": {
        "description": "开播/下播通知推送目标（umo）",
        "type": "list",
        "items": {
          "type": "string"
        },
        "hint": "在目标会话发送 /sid 获取，可填多个；QQ官方机器人主动推送有平台配额限制。"
      }
    }
  },
  "cookie_refresh": {
    "description": "Cookie自动刷新（仅Web接入支持）",
    "type": "object",
    "items": {
      "enable": {
        "description": "自动检测并刷新Cookie",
        "type": "bool",
        "default": false,
        "hint": "自动续期Cookie并即时生效，需在对应账号组填写refresh_token；刷新后浏览器端同账号登录态会失效，建议用专用小号。"
      },
      "check_interval": {
        "description": "检测间隔（小时）",
        "type": "int",
        "default": 6,
        "hint": "最小1小时。"
      }
    }
  },
  "qr_login": {
    "description": "扫码登录（登录态彻底失效时自动扫码重登）",
    "type": "object",
    "items": {
      "enable": {
        "description": "登录态彻底失效时自动推送二维码扫码重登",
        "type": "bool",
        "default": false,
        "hint": "二维码推送到『插件设置→转发目标(umo)』，扫码后自动写回配置并即时生效；需已开启Cookie自动刷新。也可发送 /bililogin 手动触发。"
      },
      "timeout_minutes": {
        "description": "单次扫码登录等待超时（分钟）",
        "type": "int",
        "default": 10,
        "hint": "超时后本轮放弃，下个检测周期会再次推送。"
      }
    }
  },
  "danmaku_send": {
    "description": "弹幕发送（仅弹幕机器人模式）",
    "type": "object",
    "items": {
      "min_interval": {
        "description": "弹幕发送最小间隔（秒）",
        "type": "float",
        "default": 1.5,
        "hint": "过快会被拒绝甚至禁言，建议不低于1.5秒。"
      },
      "max_length": {
        "description": "单条弹幕最大长度（字）",
        "type": "int",
        "default": 40,
        "hint": "上限与账号等级相关，低等级账号建议填20。"
      }
    }
  },
  "plugin_settings": {
    "description": "插件设置",
    "type": "object",
    "items": {
      "allow_message_type": {
        "description": "允许的消息类型，以英文半角逗号分隔",
        "type": "string",
        "hint": "danmaku, gift, guard_buy, super_chat, like, enter_room"
      },
      "work_mode": {
        "description": "工作模式",
        "type": "string",
        "options": [
          "danmaku_bot",
          "forward_only",
          "llm_chat_forward",
          "llm_chat_callback"
        ],
        "hint": "danmaku_bot为主模式（弹幕机器人）；其余三种为测试用途。"
      },
      "trigger_prefix": {
        "description": "弹幕触发前缀（仅弹幕机器人模式生效）",
        "type": "string",
        "default": "",
        "hint": "如填 #问 ，仅『#问 …』的弹幕触发回复；留空则全部弹幕触发，活跃直播间易积压，不建议。"
      },
      "live_persona_prompt": {
        "description": "直播间弹幕人设提示词",
        "type": "string",
        "default": "你是B站直播间的弹幕机器人「小助理」，是主播请来活跃气氛的捧哏。你性格活泼、接地气、爱接梗，把观众当朋友，熟悉直播圈和二次元文化。",
        "hint": "机器人的身份与说话风格，自动附加内置输出规则；越短越省token。"
      },
      "comment_persona_prompt": {
        "description": "视频评论区人设提示词",
        "type": "string",
        "default": "你是B站视频评论区里的热心网友「小助理」，是UP主请来陪观众聊天的头号粉丝。你熟悉B站社区文化和梗，说话自然有梗、像真人网友，对观众友好但不卑不亢。",
        "hint": "评论区人设，自动附加内置输出规则；越短越省token。"
      },
      "forward_destinations": {
        "description": "转发目标(umo)",
        "type": "list",
        "items": {
          "type": "string"
        }
      },
      "llm_chat_max_context": {
        "description": "LLM聊天最大上下文轮数",
        "type": "int",
        "default": 15,
        "hint": "实际保存条数=该值×2；弹幕场景建议3~5。"
      },
      "llm_chat_callback": {
        "description": "LLM聊天回调",
        "type": "object",
        "items": {
          "callback_url": {
            "description": "回调地址",
            "type": "string"
          },
          "callback_method": {
            "description": "回调方法",
            "type": "string",
            "options": [
              "GET",
              "POST"
            ]
          }
        }
      },
      "random_drop": {
        "description": "随机丢弃（测试用）",
        "type": "object",
        "items": {
          "enable": {
            "description": "是否启用",
            "type": "bool"
          },
          "drop_rate": {
            "description": "丢弃概率",
            "type": "float"
          }
        }
      },
      "llm_provider_id": {
        "description": "LLM聊天使用的模型供应商（留空则跟随 AstrBot 当前使用的供应商）",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "下拉选择已配置的供应商；留空跟随AstrBot当前使用的供应商。"
      },
      "llm_breaker": {
        "description": "LLM连续失败熔断（单供应商保护）",
        "type": "object",
        "items": {
          "enable": {
            "description": "是否启用",
            "type": "bool",
            "default": true,
            "hint": "连续失败达阈值后暂停LLM调用（期间弹幕/评论不回复），冷却到期自动以真实请求探测恢复。"
          },
          "threshold": {
            "description": "连续失败次数阈值",
            "type": "int",
            "default": 3,
            "hint": "异常、超时、空响应均计为失败；达到该次数即熔断。"
          },
          "cooldown": {
            "description": "熔断冷却时长（秒）",
            "type": "float",
            "default": 120,
            "hint": "冷却期内跳过所有LLM调用；熔断期间评论区轮询也暂停（不推进已读位置）。"
          }
        }
      }
    }
  }
}
